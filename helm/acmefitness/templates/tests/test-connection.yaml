apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acmefitness.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "acmefitness.name" . }}
    helm.sh/chart: {{ include "acmefitness.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "acmefitness.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
